# Инструкции по работе с Git

## 🔄 Обновление проекта в Git (Push)

После внесения изменений выполните:

```bash
# 1. Проверить статус
git status

# 2. Добавить все изменения
git add .

# 3. Создать коммит
git commit -m "Описание изменений"

# 4. Отправить в Git
git push origin main
```

**Или одной командой:**
```bash
git add . && git commit -m "Update project" && git push origin main
```

---

## ⬇️ Обновление проекта из Git (Pull)

Для получения последних изменений:

```bash
# Обновить локальную копию
git pull origin main
```

**Если нужно обновить все ветки:**
```bash
git fetch --all
git pull --all
```

---

## 📋 Примеры сообщений для коммитов

```bash
git commit -m "Add VLM annotation and training pipeline"
git commit -m "Update ensemble detector with better calibration"
git commit -m "Fix data annotation script for Roboflow dataset"
git commit -m "Add documentation and usage examples"
```

---

## ⚠️ Если возникли конфликты

```bash
# Git покажет конфликтующие файлы
# Откройте их в редакторе и разрешите конфликты вручную
# Затем:
git add .
git commit -m "Resolve merge conflicts"
git push origin main
```

---

## 🔍 Полезные команды

```bash
# Посмотреть историю коммитов
git log --oneline

# Посмотреть изменения перед коммитом
git diff

# Отменить изменения в файле (еще не закоммиченные)
git checkout -- filename

# Создать новую ветку
git checkout -b feature/new-feature
```

---

## 📝 Полная документация

Для более подробной информации см. [git_commands.md](git_commands.md)

